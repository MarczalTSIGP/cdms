(function() {
  const element_id = "form-to-sign-documet-<%= @document_signer.document_id %>";
  const element = document.getElementById(element_id);
  element.innerHTML = '<%= j render "users/document_signers/sign_form", document_id: @document_signer.document_id %>';
})();
