<% provide(:head_title, t('views.document.members.new')) %>
<% provide(:page_title, t('views.document.members.new')) %>

<% if defined?(@user) %>
    <%= render 'form_member'%>
<% else %>
    <%= render 'form'%>
<% end %>